node_modules/
dist/
coverage/
*.log
.DS_Store
*.ipa
*.xcarchive
.vitest/
playwright-report/
test-results/
e2e-evidence/
# Track .claude/ (rules, workflows, etc.), but never the local worktree dirs (local-only work)
.claude/worktree/
.claude/worktrees/
.kanna/
# C3 local cache (regenerated by `c3 repair` / `c3 init`)
.c3/c3.db

# Local secrets (e.g. GA4_MEASUREMENT_ID / GA4_API_SECRET for build) — never commit
.env
.env.local
