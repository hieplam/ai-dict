# C7 — Finish-setup toolbar badge — Hunter reports

## Task 1: `badgeStateFor` — pure domain predicate → state

**Status: BLOCKED** (work complete and proven; commit rejected by a pre-existing, unrelated
repo-wide gate failure — see below).

### Files changed (staged, NOT committed — see Blocker)

- Created `packages/app/src/domain/badge-policy.ts` — `BadgeState` interface + `badgeStateFor`
  pure function, verbatim per plan Task 1 Step 2 / design spec D2.
- Created `packages/app/test/badge-policy.test.ts` — 2 test cases, verbatim per plan Task 1
  Step 1.
- Modified `packages/app/src/index.ts` — added `export * from './domain/badge-policy';`
  immediately after the `saved-words-policy` export line (line 9 → new line 10 pushes
  `nudge-policy` down by one).

Verified `packages/app/src/domain/badge-policy.ts` imports **nothing** (no `chrome.*`, no other
`domain/` module, no `../ports`) — pure input→output, matching the plan's global constraint and
`rule-domain-purity`.

### RED — test written first, confirmed failing for the right reason

Command: `cd packages/app && bunx vitest run test/badge-policy.test.ts` (run BEFORE
`badge-policy.ts` existed):

```
 RUN  v4.1.8 /Users/todd.lam/WORK/_TestScripts/ai-dict/.claude/worktrees/c7-finish-setup-badge/packages/app

 ❯ |app| test/badge-policy.test.ts (0 test)

⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  |app| test/badge-policy.test.ts [ test/badge-policy.test.ts ]
Error: Failed to resolve import "../src/domain/badge-policy" from "test/badge-policy.test.ts". Does the file exist?
  Plugin: vite:import-analysis
  File: /Users/todd.lam/WORK/_TestScripts/ai-dict/.claude/worktrees/c7-finish-setup-badge/packages/app/test/badge-policy.test.ts:2:30
  1  |  import { describe, it, expect } from "vitest";
  2  |  import { badgeStateFor } from "../src/domain/badge-policy";
     |                                 ^
  3  |  describe("badgeStateFor (C7)", () => {
  4  |  	it("no usable key: shows the setup badge with the finish-setup title", () => {

 Test Files  1 failed (1)
      Tests  no tests
   Start at  14:54:17
   Duration  779ms
```

Correct failure mode: module doesn't exist, not a typo/bad assertion.

### GREEN — after implementing `badge-policy.ts` and the barrel export

Command: `cd packages/app && bunx vitest run test/badge-policy.test.ts`:

```
 RUN  v4.1.8 /Users/todd.lam/WORK/_TestScripts/ai-dict/.claude/worktrees/c7-finish-setup-badge/packages/app


 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  14:54:31
   Duration  591ms
```

Full `packages/app` suite (sanity check, not required by the brief but run to confirm no
regression): `cd packages/app && bunx vitest run`:

```
 Test Files  37 passed (37)
      Tests  589 passed (589)
   Start at  14:56:42
   Duration  8.71s
```

(587 pre-existing + 2 new badge-policy tests = 589, matches.)

### Gate results

`cd packages/app && bun run typecheck`:

```
$ tsc --noEmit
EXIT: 0
```
Clean — no errors. (Note: root `node_modules` was missing at task start — `bun install` was run
first to make `tsc`/`eslint`/`prettier` resolvable at all; this is environment setup, not a code
change, and installed exactly what `bun.lock`/`package.json` already pin, e.g. `prettier@^3.8.3`.)

`bun run lint` (repo root):

```
$ bun scripts/check-dep-direction.mjs && eslint .
✓ dependency direction OK — all imports point inward (ref-core-dependency-rule)
LINT EXIT: 0
```
Clean.

`bun run format:check` (repo root):

```
$ prettier --check .
Checking formatting...
[warn] packages/extension-chrome/src/manifest.json
[warn] Code style issues found in the above file. Run Prettier with --write to fix.
error: script "format:check" exited with code 1
```
**Fails — but only on `packages/extension-chrome/src/manifest.json`, a file Task 1 never touches
and is explicitly barred from touching** ("Do not touch any other file").

Confirmed my own new/changed files are individually clean:
`bunx prettier --check packages/app/src/domain/badge-policy.ts packages/app/src/index.ts packages/app/test/badge-policy.test.ts`
→ `All matched files use Prettier code style!` (exit 0).

Confirmed this failure is **pre-existing on the branch tip, unrelated to any change in this
task**: `git stash` (removing all 3 of my file changes) then `bun run format:check` from a clean
`feature/C7FinishSetupBadge` @ `8ca4b88` (= `origin/master` tip) reproduces the identical single
warning on `manifest.json`, exit 1. (I then ran `git stash pop` to restore my work, and separately
had to `git checkout -- packages/extension-chrome/src/manifest.json` after an exploratory
`prettier --write` touched it while diagnosing — confirmed reverted, `git status` shows only my 3
intended files changed, nothing else in the tree.)

Diagnostic note for the Warchief (not a fix, just what I found while isolating root cause): the
installed `prettier@^3.8.3` collapses `manifest.json`'s `"permissions": [...]` /
`"host_permissions": [...]` / `"matches": [...]` / `"js": [...]` arrays onto single lines, but the
file currently has them hand-wrapped across multiple lines. Git blame shows
`1293dd3 style(chrome): format manifest.json to satisfy format:check gate (#93)` previously
reformatted this exact file to satisfy this exact gate — so the gate and the file have drifted
apart again since then, most likely a Prettier version/behavior change, not anything in scope
here. This looks like separate, pre-existing repo debt.

### Blocker

The repo's `.githooks/pre-commit` hook runs `exec bun run format:check` (repo-wide, no path
scoping) and hard-fails the commit — `set -e` in the hook script means the non-zero exit from
`prettier --check .` aborts the commit unconditionally, regardless of which files are staged.

I attempted the exact commit the brief specifies:

```
git add packages/app/src/domain/badge-policy.ts packages/app/src/index.ts packages/app/test/badge-policy.test.ts
git commit -m "feat: finish-setup badge — add badgeStateFor pure predicate (C7)" -m $'Tribe-Card: c7-finish-setup-badge\nTribe-Task: 1/3'
```

Output:

```
$ prettier --check .
Checking formatting...
[warn] packages/extension-chrome/src/manifest.json
[warn] Code style issues found in the above file. Run Prettier with --write to fix.
error: script "format:check" exited with code 1
```

**No commit was created.** `git log --oneline -1` still shows `8ca4b88` (the pre-existing tip);
`git status` confirms the 3 task files remain staged (added/modified) but uncommitted:

```
Changes to be committed:
	new file:   packages/app/src/domain/badge-policy.ts
	modified:   packages/app/src/index.ts
	new file:   packages/app/test/badge-policy.test.ts
```

I did not use `--no-verify` (forbidden without explicit user authorization, and the brief did not
grant it) and did not touch `manifest.json` to force the gate green (out of scope — the brief says
"Do not touch any other file", and fixing an unrelated file's formatting would be scope creep past
this task's brief regardless).

**This needs the Warchief's call**, one of:
1. Authorize a separate, explicit fix to `packages/extension-chrome/src/manifest.json`'s
   formatting (either as a prerequisite hygiene commit outside this task, or added to this task's
   scope explicitly) — then re-dispatch me/a fresh Hunter to redo the commit step.
2. Confirm this is known repo debt and explicitly authorize `--no-verify` for this one commit (the
   global git safety protocol reserves that decision for explicit user/Warchief instruction, not
   for a Hunter to decide unilaterally).
3. Some other resolution the Warchief prefers.

All code/test work for Task 1 is complete, proven RED→GREEN, and staged — nothing is lost; the
next Hunter (or this one, re-dispatched) only needs to re-run the commit once the gate question is
resolved.
