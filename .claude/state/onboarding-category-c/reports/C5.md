# C5 Key Paste Hygiene — Hunter Report

Worktree: `/Users/todd.lam/WORK/_TestScripts/ai-dict/.claude/worktrees/c5-key-paste-hygiene`
Branch: `feature/C5KeyPasteHygiene`
Plan: `docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md`

## Task 1: `key-hygiene.ts` — domain module (normalize / classifyPrefix / hintFor)

Scope executed exactly per the plan's Task 1 section (Steps 1-3), nothing else.

### What I did

1. Created `packages/app/test/key-hygiene.test.ts` — the exhaustive table test file verbatim
   from the plan (28 tests across `normalize`, `classifyPrefix`, `hintFor` describe blocks).
2. Ran the test file and confirmed RED (module not found — for the right reason).
3. Created `packages/app/src/domain/key-hygiene.ts` verbatim from the plan — dependency-free
   domain module, only `import type { Provider } from './types';`, exporting `normalize`,
   `classifyPrefix`, `hintFor`, and types `KeyPrefixClass`/`KeyHint`.
4. Added the barrel-export line `export * from './domain/key-hygiene';` to
   `packages/app/src/index.ts`, right after the existing `export * from './domain/pii';` line.
5. Re-ran the test file and confirmed GREEN (28/28 passing).
6. Ran the full gate (typecheck / lint / format:check) — all clean. Note: the worktree had no
   `node_modules` (a fresh `git worktree add` doesn't link workspace deps), so I ran `bun install`
   at the worktree root before the gate would resolve `tsc`/`eslint`/`prettier` and their
   dependency graph (this is a repo-mechanics prerequisite, not a plan deviation — no files
   outside the plan's declared scope were touched).
7. Ticked Task 1's three plan checkboxes (`- [ ]` → `- [x]`) in
   `docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md` — confirmed only those three lines
   changed (Tasks 2-4 checkboxes remain unticked).
8. Staged exactly the four files the brief named and committed with the exact message/trailers
   specified. No Co-Authored-By trailer, no Claude attribution footer.

### RED proof (test run BEFORE the implementation existed)

```
$ cd packages/app && bunx vitest run test/key-hygiene.test.ts
 RUN  v4.1.8 .../packages/app

 ❯ |app| test/key-hygiene.test.ts (0 test)

⎯⎯⎯⎯⎯⎯ Failed Suites 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  |app| test/key-hygiene.test.ts [ test/key-hygiene.test.ts ]
Error: Failed to resolve import "../src/domain/key-hygiene" from "test/key-hygiene.test.ts". Does the file exist?
  Plugin: vite:import-analysis
  File: .../packages/app/test/key-hygiene.test.ts:2:51
  1  |  import { describe, it, expect } from "vitest";
  2  |  import { normalize, classifyPrefix, hintFor } from "../src/domain/key-hygiene";
     |                                                      ^
  3  |  describe("normalize", () => {

 Test Files  1 failed (1)
      Tests  no tests
```

Failed for the right reason: module not found (implementation didn't exist yet), not a typo or
bad import in the test itself.

### GREEN proof (test run AFTER the implementation)

```
$ cd packages/app && bunx vitest run test/key-hygiene.test.ts
 RUN  v4.1.8 .../packages/app

 Test Files  1 passed (1)
      Tests  28 passed (28)
   Duration  491ms
```

Re-confirmed again right before commit — still 28/28 passing.

### Gate output (verbatim)

```
$ cd packages/app && bun run typecheck
$ tsc --noEmit
(no output — clean)

$ cd .. && bun run lint
$ bun scripts/check-dep-direction.mjs && eslint .
✓ dependency direction OK — all imports point inward (ref-core-dependency-rule)
(eslint: no output — clean)

$ bun run format:check
$ prettier --check .
Checking formatting...
All matched files use Prettier code style!
```

The commit's own pre-commit hook (`.githooks/pre-commit`) also ran `prettier --check .` at
commit time and passed clean (see commit transcript below).

### Commit

```
$ git add packages/app/src/domain/key-hygiene.ts packages/app/test/key-hygiene.test.ts packages/app/src/index.ts docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md
$ git commit -m "feat: key paste hygiene — add key-hygiene domain module (C5)" -m $'Roadmap-Card: c5-key-paste-hygiene\nPlan-Task: 1/4'
$ prettier --check .
Checking formatting...
All matched files use Prettier code style!
[feature/C5KeyPasteHygiene 8741f98] feat: key paste hygiene — add key-hygiene domain module (C5)
 4 files changed, 216 insertions(+), 3 deletions(-)
 create mode 100644 packages/app/src/domain/key-hygiene.ts
 create mode 100644 packages/app/test/key-hygiene.test.ts
```

**Commit SHA:** `8741f989649929a91384b8d74e99437e797c301f`

Commit message body (verified via `git log -1 --format='%B'`):

```
feat: key paste hygiene — add key-hygiene domain module (C5)

Roadmap-Card: c5-key-paste-hygiene
Plan-Task: 1/4
```

Files changed: `docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md` (Task 1 checkboxes
only), `packages/app/src/domain/key-hygiene.ts` (new), `packages/app/src/index.ts` (+1 line),
`packages/app/test/key-hygiene.test.ts` (new).

### Notes for the Warchief

- No ambiguity encountered; the plan's Task 1 section was self-contained and matched the repo
  exactly (`packages/app/src/domain/types.ts` already exports `Provider`, `src/index.ts` already
  had the `export * from './domain/pii';` anchor line).
- One repo-mechanics step not explicit in the brief: the worktree had no `node_modules` (fresh
  `git worktree add` doesn't link workspace deps), so `bun install` was run at the worktree root
  before the gate commands would resolve `tsc`/`eslint`/`prettier`. This is a standard `bun`
  workspace prerequisite, not a scope change — no source files outside the brief's four named
  files were touched or committed.
- Task 1 only. Tasks 2-4 (onboarding UI, settings UI, e2e) are untouched, as instructed — their
  plan checkboxes remain `- [ ]`.

## Task 2: Onboarding — hint UI + normalize on activate

Scope executed exactly per the plan's Task 2 section (Steps 1-3), nothing else.

### What I did

1. Appended the 4 new hint tests to `packages/app/test/ui/onboarding-view.test.ts`, right after
   the existing `'blocks activation with an error when the key is empty (no save emitted)'` test's
   preceding neighbor — verbatim from the plan (`no hint for a realistic key`, `mismatch hint`,
   `malformed hint`, `hides the hint once cleared`).
2. Extended the existing `'emits "save" with the trimmed key and chosen language on activate'`
   test's input line from `'  AIza-real  '` to `'  "AIza-real"\n'` (quote-wrapped,
   newline-padded), keeping the same expectation `{ apiKey: 'AIza-real', targetLang: 'en' }`.
3. Ran the test file and confirmed RED: 5 failing tests (the 4 new hint tests failing with
   `Cannot read properties of null` on `#key-hint`, plus the extended save test failing because
   `apiKey` still carried the raw `'"AIza-real"'` string) — 13 pre-existing tests still passed.
4. Implemented in `packages/app/src/ui/onboarding-view.ts`, exactly per the plan's 7 sub-steps:
   - Added `import { normalize, hintFor } from '../domain/key-hygiene';`.
   - Inserted `<p id="key-hint" aria-live="polite" hidden></p>` into `MARKUP` right after
     `#key-help`.
   - Added the `#key-hint{...}` CSS rule (token-only: `var(--ad-accent)`,
     `var(--ad-accent-soft)`, `var(--ad-ink)`) right after the existing `#key-help{...}` rule.
   - Added the private `refreshKeyHint()` method right after `refreshProgress()`.
   - Extended the `key` `input` listener in `connectedCallback` to also call
     `this.refreshKeyHint()`.
   - Added `this.refreshKeyHint();` at the end of `connectedCallback` (right after
     `this.refreshProgress();`, before `key.focus();`) and inside the `value` setter (right after
     its own `this.refreshProgress();` call).
   - Updated `submit()` to compute `apiKey` via `normalize(...)` instead of the bare
     `.value.trim()`.
5. Re-ran the test file and confirmed GREEN: 18/18 passing (13 pre-existing + 4 new + 1 extended).
6. Ran the full `packages/app` Vitest suite: 619/619 passing (37 test files) — no regressions.
7. Ran the gate (typecheck / lint / format:check) — all clean.
8. Ticked Task 2's three plan checkboxes (`- [ ]` → `- [x]`) in
   `docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md` — confirmed via `git diff` that
   only those three lines changed (Tasks 1/3/4 checkboxes untouched).
9. Staged exactly the three files the brief named and committed with the exact message/trailers
   specified. No Co-Authored-By trailer, no Claude attribution footer.

### RED proof (test run BEFORE the implementation existed)

```
$ cd packages/app && bunx vitest run test/ui/onboarding-view.test.ts

 FAIL  |app| test/ui/onboarding-view.test.ts > <onboarding-view> > emits "save" with the trimmed key and chosen language on activate
AssertionError: expected { apiKey: '"AIza-real"', …(1) } to deeply equal { apiKey: 'AIza-real', …(1) }
- Expected
+ Received
  {
-   "apiKey": "AIza-real",
+   "apiKey": "\"AIza-real\"",
    "targetLang": "en",
  }

 FAIL  |app| test/ui/onboarding-view.test.ts > <onboarding-view> > shows no hint for a realistic, correctly-prefixed key (C5)
TypeError: Cannot read properties of null (reading 'hidden')

 FAIL  |app| test/ui/onboarding-view.test.ts > <onboarding-view> > shows a mismatch hint when a pasted key looks like a different provider's (C5)
TypeError: Cannot read properties of null (reading 'hidden')

 FAIL  |app| test/ui/onboarding-view.test.ts > <onboarding-view> > shows a malformed hint for an implausibly short pasted key (C5)
TypeError: Cannot read properties of null (reading 'hidden')

 FAIL  |app| test/ui/onboarding-view.test.ts > <onboarding-view> > hides the hint again once the field is cleared (C5)
TypeError: Cannot read properties of null (reading 'hidden')

 Test Files  1 failed (1)
      Tests  5 failed | 13 passed (18)
```

Failed for the right reasons: `#key-hint` doesn't exist yet (querySelector returns `null`), and
the raw `.trim()` in `submit()` doesn't strip the wrapping quotes/newline — not typos or bad
imports.

### GREEN proof (test run AFTER the implementation)

```
$ cd packages/app && bunx vitest run test/ui/onboarding-view.test.ts
 Test Files  1 passed (1)
      Tests  18 passed (18)
   Duration  853ms

$ cd packages/app && bunx vitest run
 Test Files  37 passed (37)
      Tests  619 passed (619)
   Duration  4.32s
```

### Gate output (verbatim)

```
$ cd packages/app && bun run typecheck
$ tsc --noEmit
(no output — clean)

$ cd .. && bun run lint
$ bun scripts/check-dep-direction.mjs && eslint .
✓ dependency direction OK — all imports point inward (ref-core-dependency-rule)
(eslint: no output — clean)

$ bun run format:check
$ prettier --check .
Checking formatting...
All matched files use Prettier code style!
```

The commit's own pre-commit hook (`.githooks/pre-commit`) also ran `prettier --check .` at
commit time and passed clean (see commit transcript below).

### Commit

```
$ git add packages/app/src/ui/onboarding-view.ts packages/app/test/ui/onboarding-view.test.ts docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md
$ git commit -m "feat: key paste hygiene — hint + normalize the onboarding key field (C5)" -m $'Roadmap-Card: c5-key-paste-hygiene\nPlan-Task: 2/4'
$ prettier --check .
Checking formatting...
All matched files use Prettier code style!
[feature/C5KeyPasteHygiene 8c66875] feat: key paste hygiene — hint + normalize the onboarding key field (C5)
 3 files changed, 67 insertions(+), 6 deletions(-)
```

**Commit SHA:** `8c668752b6921cd72477c4b97cc86fdfb93b73fe`

Commit message body (verified via `git log -1 --format='%B'`):

```
feat: key paste hygiene — hint + normalize the onboarding key field (C5)

Roadmap-Card: c5-key-paste-hygiene
Plan-Task: 2/4
```

Files changed: `docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md` (Task 2 checkboxes
only), `packages/app/src/ui/onboarding-view.ts` (import, markup, CSS, `refreshKeyHint()`,
wired listeners, `submit()` normalize), `packages/app/test/ui/onboarding-view.test.ts` (4 new
tests + 1 extended test).

### Notes for the Warchief

- No ambiguity encountered; the plan's Task 2 section matched the repo exactly (existing
  `#key-help` anchor line, existing `refreshProgress()` method, existing `key.addEventListener`
  and `value` setter call sites all present as the plan described).
- Task 2 only. Task 1 stays committed as-is; Tasks 3-4 (settings UI, e2e) are untouched — their
  plan checkboxes remain `- [ ]`.

## Task 3

Scope executed exactly per the plan's Task 3 section (Steps 1-3), nothing else.

### What I did

1. Read the current `packages/app/src/ui/settings-form.ts` and
   `packages/app/test/ui/settings-form.test.ts` to confirm the plan's anchors matched reality
   (`#key-help` line before `#env-notice`, `#key-help,#tpl-help{...}` CSS rule, `syncKeyField()` /
   `commitKeyField()` methods, the `describe('<settings-form> provider selection', ...)` block's
   closing `});`) — all present exactly as the plan described, no deviation needed.
2. Appended the new `describe('<settings-form> key paste hygiene (C5)', ...)` block (6 tests)
   verbatim from the plan, right after the `<settings-form> provider selection` describe block's
   closing `});` and before `<settings-form> error-reporting toggle`.
3. Ran `cd packages/app && bunx vitest run test/ui/settings-form.test.ts` and confirmed RED — all
   6 new tests failed for the right reason (4 for `#key-hint` not existing yet — "Cannot read
   properties of null (reading 'hidden')" — and 2 for the padded/quoted key round-tripping
   unnormalized); 60 pre-existing tests in that file still passed.
4. Implemented in `packages/app/src/ui/settings-form.ts`:
   - Added `import { normalize, hintFor } from '../domain/key-hygiene';`.
   - Inserted `<p id="key-hint" aria-live="polite" hidden></p>` into `MARKUP`, between `#key-help`
     and `#env-notice`.
   - Added the `#key-hint{...}` CSS rule (token-only: `var(--ad-accent)` /
     `var(--ad-accent-soft)` / `var(--ad-ink)`, matching `#key-help`/`.env-notice`'s own token
     usage) right after the existing `#key-help,#tpl-help{...}` rule.
   - Added the private `refreshKeyHint()` method right after `syncKeyField()`'s body, and called
     `this.refreshKeyHint();` as the last line of `syncKeyField()` (so every existing call site —
     provider-change listener, `value` setter hydration, `connectedCallback`'s own
     `syncKeyField()` call — picks up the hint refresh for free).
   - Added `key.addEventListener('input', () => this.refreshKeyHint());` next to the existing
     `focus`/`blur` key listeners in `connectedCallback`.
   - Updated `commitKeyField()` to stash `normalize(this.q<HTMLInputElement>('#key').value)`
     instead of the raw `.value`.
5. Ran `cd packages/app && bunx vitest run test/ui/settings-form.test.ts` again and confirmed
   GREEN — all 66 tests passed (60 pre-existing + 6 new).
6. Ran the full `packages/app` Vitest suite (`bunx vitest run`) — 625/625 passed across 37 files
   (no regressions elsewhere from the shared `key-hygiene.ts` import or the new listener/method).
7. Ran the gate: `cd packages/app && bun run typecheck` (clean, `tsc --noEmit` no output) →
   `cd .. && bun run lint` (clean: dependency-direction check + eslint, no findings) →
   `bun run format:check` (clean: "All matched files use Prettier code style!").
8. Ticked Task 3's three plan checkboxes (`- [ ]` → `- [x]`) in
   `docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md` — only Task 3's three lines
   changed; Task 4's checkboxes remain `- [ ]`.
9. Staged exactly the three files the brief named
   (`packages/app/src/ui/settings-form.ts`, `packages/app/test/ui/settings-form.test.ts`,
   `docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md`) and committed with the exact
   message/trailers specified in the brief.

### RED proof (verbatim, truncated to the failure summary)

```
 ❯ |app| test/ui/settings-form.test.ts (66 tests | 6 failed) 662ms
     × shows no hint for a realistic, correctly-prefixed Gemini key 4ms
     × shows a mismatch hint when an Anthropic-shaped key is typed while Gemini is selected 2ms
     × re-evaluates the hint against the now-visible provider on switch 1ms
     × never shows a hint while the Gemini field is env-locked, even for a bad stashed key 1ms
     × emits a normalized apiKey when the pasted value has padding/quotes 3ms
     × normalizes a stashed key across a provider switch, not just on same-provider save 2ms

 FAIL  |app| test/ui/settings-form.test.ts > ... > shows no hint for a realistic, correctly-prefixed Gemini key
TypeError: Cannot read properties of null (reading 'hidden')
 ❯ test/ui/settings-form.test.ts:501:19

 FAIL  |app| test/ui/settings-form.test.ts > ... > emits a normalized apiKey when the pasted value has padding/quotes
AssertionError: expected '  "AIzaSyABCDEFGHIJKLMNOPQRSTUVWXYZ01…' to be 'AIzaSyABCDEFGHIJKLMNOPQRSTUVWXYZ01234'
Expected: "AIzaSyABCDEFGHIJKLMNOPQRSTUVWXYZ01234"
Received: "  "AIzaSyABCDEFGHIJKLMNOPQRSTUVWXYZ01234"  "

 Test Files  1 failed (1)
      Tests  6 failed | 60 passed (66)
```

### GREEN proof (verbatim)

```
$ bunx vitest run test/ui/settings-form.test.ts
 Test Files  1 passed (1)
      Tests  66 passed (66)

$ bunx vitest run   (full packages/app suite)
 Test Files  37 passed (37)
      Tests  625 passed (625)
```

### Gate output (verbatim)

```
$ cd packages/app && bun run typecheck
$ tsc --noEmit
(no output — clean)

$ cd .. && bun run lint
$ bun scripts/check-dep-direction.mjs && eslint .
✓ dependency direction OK — all imports point inward (ref-core-dependency-rule)
(eslint: no findings)

$ bun run format:check
$ prettier --check .
Checking formatting...
All matched files use Prettier code style!
```

### Commit

- SHA: `a26e76278c13fb5d1ea19e4451d921ff941b9ac2`
- Message:
  ```
  feat: key paste hygiene — hint + normalize the settings key field (C5)

  Roadmap-Card: c5-key-paste-hygiene
  Plan-Task: 3/4
  ```
- Files: `packages/app/src/ui/settings-form.ts`, `packages/app/test/ui/settings-form.test.ts`,
  `docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md` (Task 3 checkboxes only).

### Notes for the Warchief

- No ambiguity encountered; the plan's Task 3 section matched the repo exactly (existing
  `#key-help` anchor line before `#env-notice`, existing `#key-help,#tpl-help{...}` CSS rule,
  existing `syncKeyField()`/`commitKeyField()` methods, existing `key` focus/blur listener block,
  existing `describe('<settings-form> provider selection', ...)` block all present as the plan
  described).
- Task 3 only. Tasks 1-2 stay committed as-is; Task 4 (e2e) is untouched — its plan checkboxes
  remain `- [ ]`.

## Task 4: e2e functional test

Scope executed exactly per the plan's Task 4 section (Steps 1-3), nothing else, with one
explicitly-authorized deviation from the brief (build command — see below).

### What I did

1. Read the plan's Task 4 section in full, plus the pre-existing
   `packages/extension-chrome/e2e/onboarding.spec.ts` (the model file) and confirmed
   `storageDump` exists in `packages/extension-chrome/e2e/helpers.ts:62`.
2. Created `packages/extension-chrome/e2e/c5-key-hygiene.spec.ts` verbatim from the plan's Task 4
   Step 1 content — a `describe('C5 key paste hygiene', ...)` block with the two specified tests:
   - `'a padded, quote-wrapped key pasted in onboarding is stored fully cleaned'` — fills the
     onboarding `#key` field with `'  "AIzaSyABCDEFGHIJKLMNOPQRSTUVWXYZ01234"  \n'`, activates,
     and asserts the stored `settings.apiKey` (via `storageDump`) equals the fully-cleaned
     `'AIzaSyABCDEFGHIJKLMNOPQRSTUVWXYZ01234'`.
   - `"an OpenAI-shaped key pasted into the onboarding Gemini field shows a live mismatch hint"` —
     fills `#key` with an OpenAI-shaped `sk-...` value, asserts `#key-hint` is visible and
     contains both `'OpenAI'` and `'Gemini'`, then clicks `#activate` and asserts it still
     proceeds to `settings-form` (the hint never blocks activation).
3. **Deviation from the plan's literal Step 2 build command, per explicit brief instruction:**
   the plan text says `unset GEMINI_API_KEY` then `bun run build:chrome`. Per the Warchief's
   brief, I used the project's e2e-safe build script instead: `bun run build:chrome:e2e` (script
   defined in root `package.json`: `"build:chrome:e2e": "bun run --filter
   '@ai-dict/extension-chrome' build:e2e"`), run from the worktree root. This produced a clean
   build with no baked `GEMINI_API_KEY`, satisfying the same underlying constraint the plan's
   Step 2 text was protecting (onboarding must render, not skip straight to settings).
4. Ran `cd packages/extension-chrome && bunx playwright test c5-key-hygiene.spec.ts` — 2/2 passed.
5. Ran `cd packages/extension-chrome && bunx playwright test onboarding.spec.ts` as the regression
   check named in the brief — 3/3 passed, confirming the plain activation flow is unaffected and
   this branch's `onboarding.spec.ts` matches what the brief said to expect (no C1-specific third
   test present on this branch; it is whatever ships on master).
6. Ran the gate from the worktree root: `bun run lint` (clean — dependency-direction check +
   eslint, no findings) and `bun run format:check` (clean — "All matched files use Prettier code
   style!").
7. Ticked Task 4's three plan checkboxes (`- [ ]` → `- [x]`) in
   `docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md` — confirmed via `git status`/`git
   diff` that only those three lines changed (Tasks 1-3 checkboxes, already `- [x]`, untouched).
8. Staged exactly the two files the brief named
   (`packages/extension-chrome/e2e/c5-key-hygiene.spec.ts`,
   `docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md`) — confirmed via `git status
   --porcelain` before commit that no `dist/` build artifacts or other files were staged — and
   committed with the exact message/trailers specified in the brief. No Co-Authored-By trailer,
   no Claude attribution footer.

### Build command used (verbatim, actually run)

```
$ cd /Users/todd.lam/WORK/_TestScripts/ai-dict/.claude/worktrees/c5-key-paste-hygiene
$ bun run build:chrome:e2e
$ bun run --filter '@ai-dict/extension-chrome' build:e2e
@ai-dict/extension-chrome build:e2e: $ bun ../../scripts/check-dep-direction.mjs && bun esbuild.config.mjs
@ai-dict/extension-chrome build:e2e: ✓ dependency direction OK — all imports point inward (ref-core-dependency-rule)
@ai-dict/extension-chrome build:e2e:
@ai-dict/extension-chrome build:e2e:   dist/sw.js  447.8kb
@ai-dict/extension-chrome build:e2e:
@ai-dict/extension-chrome build:e2e: ⚡ Done in 97ms
@ai-dict/extension-chrome build:e2e:
@ai-dict/extension-chrome build:e2e:   dist/content-elements.js  436.0kb
@ai-dict/extension-chrome build:e2e:
@ai-dict/extension-chrome build:e2e: ⚡ Done in 25ms
@ai-dict/extension-chrome build:e2e:
@ai-dict/extension-chrome build:e2e:   dist/content.js  448.4kb
@ai-dict/extension-chrome build:e2e:
@ai-dict/extension-chrome build:e2e: ⚡ Done in 25ms
@ai-dict/extension-chrome build:e2e:
@ai-dict/extension-chrome build:e2e:   dist/options.js  437.7kb
@ai-dict/extension-chrome build:e2e:
@ai-dict/extension-chrome build:e2e: ⚡ Done in 26ms
@ai-dict/extension-chrome build:e2e:
@ai-dict/extension-chrome build:e2e:   dist/side-panel.js  436.0kb
@ai-dict/extension-chrome build:e2e:
@ai-dict/extension-chrome build:e2e: ⚡ Done in 25ms
@ai-dict/extension-chrome build:e2e: Exited with code 0
```

### `c5-key-hygiene.spec.ts` playwright output (verbatim)

```
$ cd packages/extension-chrome && bunx playwright test c5-key-hygiene.spec.ts
Running 2 tests using 1 worker

  ✓  1 e2e/c5-key-hygiene.spec.ts:5:3 › C5 key paste hygiene › a padded, quote-wrapped key pasted in onboarding is stored fully cleaned (2.9s)
  ✓  2 e2e/c5-key-hygiene.spec.ts:24:3 › C5 key paste hygiene › an OpenAI-shaped key pasted into the onboarding Gemini field shows a live mismatch hint (1.6s)

  2 passed (5.0s)
```

### Onboarding regression check — playwright output (verbatim)

```
$ cd packages/extension-chrome && bunx playwright test onboarding.spec.ts
Running 3 tests using 1 worker

  ✓  1 e2e/onboarding.spec.ts:7:1 › onboarding: activating with a key swaps to the settings screen and persists it (1.9s)
  ✓  2 e2e/onboarding.spec.ts:33:1 › onboarding: empty key shows an error and never leaves the onboarding screen (1.5s)
  ✓  3 e2e/onboarding.spec.ts:45:1 › no-key card shows the setup invite and "Open Settings" opens the options page (3.1s)

  3 passed (7.0s)
```

Confirms the pre-existing onboarding suite is unaffected by the C5 changes — it was already
green on this branch (Tasks 1-3 already committed), and stays green after Task 4.

### Gate output (verbatim, run from worktree root)

```
$ bun run lint
$ bun scripts/check-dep-direction.mjs && eslint .
✓ dependency direction OK — all imports point inward (ref-core-dependency-rule)
(eslint: no findings)

$ bun run format:check
$ prettier --check .
Checking formatting...
All matched files use Prettier code style!
```

The commit's own pre-commit hook (`.githooks/pre-commit`) also ran `prettier --check .` at
commit time and passed clean (see commit transcript below).

### Commit

```
$ git add packages/extension-chrome/e2e/c5-key-hygiene.spec.ts docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md
$ git commit -m "feat: key paste hygiene — add e2e coverage for paste cleanup + live hint (C5)" -m $'Roadmap-Card: c5-key-paste-hygiene\nPlan-Task: 4/4'
$ prettier --check .
Checking formatting...
All matched files use Prettier code style!
[feature/C5KeyPasteHygiene 2410cf6] feat: key paste hygiene — add e2e coverage for paste cleanup + live hint (C5)
 2 files changed, 48 insertions(+), 3 deletions(-)
 create mode 100644 packages/extension-chrome/e2e/c5-key-hygiene.spec.ts
```

**Commit SHA:** `2410cf670694e9f273b70f715a4fc2047917eed`

Commit message body (verified via `git log -1 --format='%B'`):

```
feat: key paste hygiene — add e2e coverage for paste cleanup + live hint (C5)

Roadmap-Card: c5-key-paste-hygiene
Plan-Task: 4/4
```

Files changed: `packages/extension-chrome/e2e/c5-key-hygiene.spec.ts` (new, 2 tests),
`docs/superpowers/plans/2026-07-16-c5-key-paste-hygiene.md` (Task 4 checkboxes only). No `dist/`
or other build artifacts were staged or committed — verified via `git status --porcelain`
immediately before `git add`.

### Notes for the Warchief

- **Deviation from plan text, authorized by the brief:** used `bun run build:chrome:e2e` instead
  of the plan's literal `unset GEMINI_API_KEY && bun run build:chrome` sequence, per the brief's
  explicit "standing campaign ruling" instruction. This is documented above with the exact
  command and output for traceability. No other deviation from the plan's Task 4 content.
- Did not run the plan's "Final gate" section (full `bun run test`, both-package typecheck, combined
  `playwright test onboarding c5-key-hygiene` run) — the brief's own instructions for Task 4 named
  only the c5-key-hygiene run, the separate onboarding regression run, and lint/format as the
  required gates, so that is exactly what was executed. Flagging this in case the Warchief wants
  the full final-gate sweep run separately before opening the PR.
- Task 4 only, and card C5's plan is now fully ticked (Tasks 1-4 all `- [x]`). No PR was opened —
  that is the Warchief's step.
