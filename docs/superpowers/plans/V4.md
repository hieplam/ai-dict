# Plan V4 — e2e gap closure

Spec: `docs/superpowers/specs/2026-08-03-v4-e2e-gaps.md`. Depends on V1 merged. Playwright
harness rules apply (bundled Chromium only; never installed Chrome; specs never fetch the live
site).

## Task 1 — persistent-profile harness variant

Extend `packages/extension-chrome/e2e/fixtures.ts` with an opt-in variant: real temp
`userDataDir` (cleaned up after), exposing a `relaunch()` affordance (close context → relaunch
same dir → re-derive extension id, which MAY change across relaunches — handle by re-deriving,
never caching). Keep the default ephemeral path untouched (zero behavior change for existing 46+
specs — prove by full suite run).

## Task 2 — restart-persistence.spec.ts

Seed settings + save a word + perform a lookup → relaunch → assert settings intact, saved word
intact, and a fresh mocked lookup succeeds (SW cold start). 3 consecutive full-suite green runs
(repo zero-flake wall) before PR.

## Task 3 — landing-drift fix

In the c11 spec (and c3 spec if it duplicates too): delete the inline `INSTALL_STATE_SCRIPT`
copy; `readFileSync` the real `docs/index.html`, extract the install-state `<script>` (anchor on
a stable marker; if extraction returns nothing → `throw` with a message naming the drift).
Negative proof: mutate the block locally, watch the spec fail, revert — document in PR body.

## Task 4 — hostile fixtures + specs

Fixture A: page served with restrictive CSP header via route interception (`script-src 'none'`
or similarly strict) → select → Define → card renders. Fixture B: page JS with competing
`mouseup`/`selectionchange` listeners calling `stopPropagation` — same assertion. Add both to
the case inventory per its bookkeeping rules.

## Task 5 — inventory + verification + PR

Update `docs/testing/e2e-case-inventory.md` (respect its frozen denominators/sign-off rules —
ADDING cases is the allowed direction). Full gates; 3 consecutive green e2e runs; PR "Testing
performed" with the negative-proof note. Merge after `gh pr checks` green.
