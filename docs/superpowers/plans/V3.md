# Plan V3 — S3 refactor, S4 sink audit, wire-flatten contract test

Spec: `docs/superpowers/specs/2026-08-03-v3-code-touching-gates.md`. Depends on V2 merged.
Strict TDD throughout; security-touching — e2e proof mandatory.

## Task 1 — S3 refactor to classifyInbound

Read `classifyInbound`'s current signature/behavior (it lives in the app core; `c3 lookup` the
file first). Refactor `packages/extension-chrome/src/content.ts` (~226) and
`packages/extension-chrome/src/side-panel.ts` (~248) from hand-rolled `sender.id !==` checks to
`classifyInbound`; add a thin variant if the SW-shaped signature doesn't fit — behavior
identical (same accept/reject set; write a unit test asserting parity BEFORE refactoring).
Check `packages/extension-safari/src/` for equivalent sites; treat identically. Re-run the
message-flow e2e (lookup, keyboard-commands, side-panel specs) — green, listed in PR.

## Task 2 — check-msg-gate.mjs (TDD)

Lock-test fixtures: `onMessage.addListener(` with `classifyInbound(` within the window (pin
window size in the test, suggest 10 lines) → green; without → red; hand-rolled `sender.id`
idiom → RED (ratified: no alternate idiom). Implement; real tree must pass after Task 1.

## Task 3 — S4 sink audit + annotations

`git grep -nE '\.(innerHTML|outerHTML)\s*=|insertAdjacentHTML\('` — enumerate every hit
(14 at audit time). Per hit: static/own-template → add `// s4: static-template — <reason>`;
model/remote-derived without sanitize → REAL violation, fix through `sanitizeMarkdown()`/
`SafeHtml`. Record the full audit table in the PR body.

## Task 4 — check-safe-html.mjs (TDD)

Lock tests: sink line + `// s4:` annotation → green; sink line referencing the sanitize path →
green; bare sink line → red. Implement; real tree passes after Task 3.

## Task 5 — wire-flatten contract test

In the wire-schema suite (runs under the `test-contract` CI job): enumerate the router's error
reply constructors (read `router.ts` / error-mapper), for each: build reply →
`JSON.parse(JSON.stringify(reply))` → assert enumerable `code`, `message`, `retryable`.
Also one negative control: a raw `new Error("x")` demonstrably LOSES `message` over the same
round-trip (documents why the rule exists).

## Task 6 — full verification + PR

Full gates + explicit message-flow e2e list in "Testing performed". Merge after `gh pr checks`
green.
