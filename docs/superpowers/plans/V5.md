# Plan V5 — the explicit verification loop doc

Spec: `docs/superpowers/specs/2026-08-03-v5-verification-loop-doc.md`. Depends on V2+V3+V4
merged — this card documents REALITY, so every claim is verified by running the gate, not by
copying the spec.

## Task 1 — gather ground truth

Fresh `master` checkout in worktree. Run every gate; record exact commands and what each failed
on when planted with a violation (reuse lock-test fixtures knowledge from V2/V3 PRs). List the
final scanner inventory from `ls scripts/hard-rule/`.

## Task 2 — CLAUDE.md "Verification loop" section

Author per spec: gate registry table (gate → command → proves), hard/soft boundary (folder +
named contract tests = hard registry; "adapters thin and decision-free" listed soft/review),
merge gate (`gh pr checks` all green before merge — branch-protection substitute), flake-triage
protocol (reproduce on master HEAD before waving off; evidence in PR body), dep-audit
advisory-by-design note, pointer to `docs/testing/e2e-case-inventory.md`. Keep it tight — a
table and five short rules, not an essay.

## Task 3 — C3 changes (CLI only)

`c3 add adr <slug>` (the change-unit) → `c3 add ref verification-loop` authored to the ref
canvas (`c3 schema ref` FIRST) → change-unit patches for the 5 rule entities updating their
Enforcement sections to name their scanner/contract test (read each rule's current body with
`--cite` before patching) → `c3 change apply` → `c3 check` green. Wire the ref to the entities
it governs.

## Task 4 — workflow-conventions.md

Add to Always: merge gate line + flake-triage line. Imperative mirror only — CLAUDE.md stays
the narrative source.

## Task 5 — verification + PR

`c3 check`, every documented command re-run verbatim from the new doc (the doc IS the test
script), format/lint gates. PR "Testing performed": list each documented gate command executed.
Merge after `gh pr checks` green.
