# Plan V2 — hard-rule scanner folder

Spec: `docs/superpowers/specs/2026-08-03-v2-hard-rule-folder.md`. Strict TDD: every scanner's
lock test is written FIRST (fixture with violation → red), then the scanner (→ green).

## Task 1 — folder + runner + exclusions format

- [x] `scripts/hard-rule/run-all.mjs`: enumerate `scripts/hard-rule/check-*.mjs`, spawn each with
      `bun`, aggregate, print per-scanner verdicts, exit 1 if any failed.
- [x] `scripts/hard-rule/exclusions.json` (or .mjs if comments needed): shape
      `{ "<rule-id>": [ { "path": "...", "reason": "..." } ] }`.
- [x] Tests first: runner discovery test (a temp scanner in the folder gets executed); exclusions
      lock test (entry without `reason` → hard failure of the runner).

## Task 2 — migrate check-dep-direction

- [x] Move `scripts/check-dep-direction.mjs` + `scripts/check-dep-direction.test.ts` into the folder (adapt to the exclusions/runner conventions ONLY if zero behavior change; otherwise leave its internals alone). Update: root `package.json` `lint` script → `bun scripts/hard-rule/run-all.mjs && eslint .`; both packages' `esbuild.config.mjs` (or build scripts) → run-all instead of the old path; any other `git grep check-dep-direction` hit. Lock test still green; a planted violation still blocks lint AND build.

## Task 3 — check-token-law.mjs (TDD)

- [x] Lock-test fixtures: hex literal / `oklch(` / `prefers-color-scheme` in a fake component →
      red; same content in `tokens.ts`-pathed fixture → green; excluded path with reason → green.
- [x] Then implement. Run against the REAL tree: expected findings =
      `packages/extension-chrome/src/sw.ts` badge hex → seed exclusion with the ratified reason.
      Any OTHER hit: judge — real violation (fix if one-line) or reasoned exclusion; document
      each in the PR body.

## Task 4 — check-key-isolation.mjs (TDD)

- [x] Read the existing selector in `eslint.config.mjs` to copy its exact allowlist (per-extension
      `sw.ts`, `options.ts`). Lock test: `.storage.local` in a non-allowlisted fixture → red.
- [x] Implement; real-tree run must be clean. ESLint selector stays (IDE feedback) — add a
      one-line comment in eslint.config.mjs marking the scanner as authoritative.

## Task 5 — check-core-agnostic.mjs (TDD)

- [x] Lock test: `chrome.` usage in a `packages/app/src/**`-pathed fixture → red. Implement, then run
      on the real tree. Hits: trivial fix in-card; non-trivial → STOP, report NEEDS_DIRECTION with the
      evidence (do not guess). Type-only `typeof chrome` occurrences: narrow the pattern or exclude
      with reason — record which.

## Task 6 — pre-commit hook

`.githooks/pre-commit`: after `format:check`, run `bun run lint`. Inspect `pre-commit.local`
first (currently byte-identical); mirror the change if that is its role. Verify with a staged
planted violation: commit refused.

## Task 7 — full verification + PR

Full gate run (typecheck, lint, format, unit, builds, e2e). PR "Testing performed" must include:
per-scanner lock-test counts, the real-tree scan results, and the planted-violation
demonstrations. Merge after `gh pr checks` green.
