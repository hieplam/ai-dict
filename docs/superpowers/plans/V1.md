# Plan V1 — e2e & CI cleanup

Spec: `docs/superpowers/specs/2026-08-03-v1-e2e-ci-cleanup.md`. Worktree under
`.claude/worktrees/`. TDD does not apply to deletions; the "test" is the surviving suite.

## Task 1 — delete the 10 spec files

In `packages/extension-chrome/e2e/`: delete `evidence.spec.ts`, `context-bank-demo.spec.ts`,
`define-fix-demo.spec.ts`, `a4-evidence.spec.ts`, `a8-evidence.spec.ts`, `a16-evidence.spec.ts`,
`b1-evidence.spec.ts`, `b2-evidence.spec.ts`, `b5-evidence.spec.ts`, `b7-evidence.spec.ts`.
Then `git grep -n` each basename repo-wide; remove dead references (playwright config lists,
helper imports, docs/testing/e2e-case-inventory.md rows if it enumerates them — follow that
file's own bookkeeping rules when editing it).

## Task 2 — env-gate readme-demo.spec.ts

Copy the exact `test.skip(!process.env.PLAYWRIGHT_RUN_*, '...')` guard pattern from
`media-kit.spec.ts`; flag name `PLAYWRIGHT_RUN_README_DEMO`. Verify: `bunx playwright test
readme-demo` reports skipped without the flag, runs with it.

## Task 3 — ci.yml surgery

Remove the `PLAYWRIGHT_RUN_LOOKUP_E2E` env line; delete jobs `test-component` and `sonarcloud`
wholesale; fix every `needs:` chain that referenced them (grep the file). Leave `dep-audit`
untouched. Validate YAML (e.g. `bunx yaml` parse or actionlint if available).

## Task 4 — full verification + PR

`bun run typecheck && bun run lint && bun run format:check && bun run test &&
bun run build:chrome:e2e && bun run e2e:chrome`. Open PR with "Testing performed" (counts,
suites, note which jobs disappeared and why — link the spec). Merge per campaign merge policy
after `gh pr checks` all green.
