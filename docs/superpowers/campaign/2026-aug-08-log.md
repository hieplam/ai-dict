# 2026-aug-08 campaign log diary — outstanding-17

> **Append-only event log** of the 2026-08-08 orchestration session, kept so the owner can
> replay what happened and judge whether the tribe workflow needs improvement. Author: the
> orchestrating session acting with Shaman authority (owner directive below). Times are local
> (UTC+7) unless marked.

**Owner directive (verbatim intent):** implement ALL outstanding ideas one by one; fresh
Warchief per card via the campaign orchestration runner; MUST follow tribe workflow with 2
lens reviews per card; capture unwritten conventions when found; the session acts as Shaman
and rules on escalated unwritten conventions; this log diary is append-only and persisted in
git; loop every 15m and do not stop until all ideas are done.

---

## Events

- **[setup] Session start.** Invoked `orchestrate-campaign` + `loop` skills before any action.
  Effort level: high.
- **[discovery] Outstanding ideas identified = 17 roadmap cards.** Owner-ratified 2026-07-16
  sequence (`docs/superpowers/campaign/2026-07-16-run-the-roadmap.md`) minus everything
  shipped through learning-spine-5 (B3/B4/A6/A10/A15, PRs #173–#178): remaining sequence
  **B6 → B9 → A1 → A3 → B13 → A2 → A5 → A13 → A14 → A7 → A12 → B14 → B11 → B10 → B12 → B15 →
  A11-spike**. Cross-checked against commit `2528486` ("restore 17 unshipped cards' plan/spec
  pairs from archive") — the two lists match exactly.
- **[discovery] Specs+plans already on master** for all 17 cards (2026-07-17 authoring wave,
  restored by PR #181). Stage-A authorship mode therefore `planning.mode: shaman` (adopted) —
  no new planning PR needed; Stage A step 5 (land specs/plans) is already satisfied.
- **[discovery] Pre-session repo state:** master @ `224b6d6`; two unrelated pre-existing
  worktrees present (`advanced-prompt-konami`, `unred-format-gate`) — not touched by this
  campaign, noted for the owner.
- **[decision · Shaman] Campaign slug `outstanding-17`**, home =
  `$(tribe-home.sh <repo>)/campaigns/outstanding-17` (machine-local, never committed).
- **[decision · Shaman] Executor model = opus.** Most recent owner-ratified executor tier
  (learning-spine-5 snapshot); supersedes the older "Warchief = Sonnet" ruling from
  2026-07-16.
- **[decision · Shaman] mergePolicy = regular** (regular merge commits only, no squash; merge
  only after `gh pr checks` fully green — repo CLAUDE.md merge gate).
- **[decision · Shaman] schemaLockPaths = `packages/app/src/ports.ts` only.** Rationale:
  learning-spine-5's R10/R13 showed whole-file locks on `domain/types.ts` mostly produce
  bookkeeping false-trips (runner gap #3: schema guard diffs `baseSha → master`, not the PR
  diff). The E1 saved-word entry shape stays protected by `ownerOnlyEscalations`
  (irreversible-data-shape) + the mandatory two-skinner audit per card.
- **[decision · Shaman] dependsOn declared** only where cards genuinely collide:
  A3←A1, A2←A1+A3 (all three rework the answer-card render path), B11←B6, B12←B6 (words-page
  surfaces). Everything else rides the sequence order.
- **[decision · Shaman] ownerOnlyEscalations** = the roadmap §6 register verbatim
  (irreversible-data-shape, product-promise-change, new-permission-or-trust-surface,
  privacy-surface-change) + explicit E3 (advertising non-English), E4 (PDF go/no-go), E6 (any
  new manifest permission). E2 (backup envelope) is owner-ratified already; A12 builds under
  E3 "build-don't-advertise".
- **[decision · Shaman] Log diary lives at
  `docs/superpowers/campaign/2026-aug-08-log.md`** on branch
  `docs/2026-aug-08-campaign-log` (worktree `.claude/worktrees/campaign-log`), committed at
  every meaningful checkpoint, PR'd to master as docs-only at campaign end (and mid-campaign
  if the owner wants an early look). Reason: repo rules require worktree + PR flow even for
  docs; the file cannot sit uncommitted on master.
- **[friction · workflow] First diary commit rejected by the pre-commit hook.** A fresh
  worktree has no `node_modules`, so ESLint's typed rules can't resolve imports and report
  hundreds of `no-unsafe-*` false errors — even for a docs-only diff (the hook runs
  repo-wide `lint`, not diff-scoped). Known trap (also bit the 2026-08-02 workflow-tool
  campaign: "pre-create worktrees + bun install"). Fixed with `bun install` in the worktree;
  commit retried. **Unwritten-convention candidate #1:** "every new worktree requires
  `bun install` before any commit" is enforced nowhere in writing — CLAUDE.md mandates
  worktrees but never says this. Ruling to follow at wrap-up.
- **[preflight] `doctor.sh` all green** (bun 1.3.14, gh authenticated, Agent SDK
  credentials, runner deps installed).
- **[decision · Shaman] Standing directive R0 written to `answers.md`:** every card MUST get
  the tribe workflow's two independent skinner lens reviews; any reviewer-flagged convention
  that has no written rule is an **unwritten-convention event** — the Warchief escalates it,
  the Shaman rules, and the ruling is captured here + in `answers.md` (and, where durable, as
  a repo rule via the proper governance path). This is the workflow property the owner
  explicitly wants exercised and observed this session.
- **[launch] Runner dry-run OK** — derived next action `{cardId: B6, phase: fresh}`, exactly
  the head of the sequence. Zero side effects confirmed.
- **[launch] Real runner launched in background** (~15:42 UTC, task `ba8ozdf1k`):
  `bun <runner>/run.ts --repo <repo> --model opus --home <tribe-home>/campaigns/outstanding-17`.
  Within a minute the state file showed **B6 and B9 both `running`** (concurrent executor
  sessions, same behavior as learning-spine-5) from base `224b6d6`. A 15-minute cron loop is
  armed as the fallback heartbeat (survives usage-limit pauses); the background-exit
  notification is the primary wake signal for Stage C round-trips.
- **[incident · 15:43 UTC] First launch died in 36s — exit 3, ALL 13 spawned sessions dead.**
  Evidence from the B6 session log: `"apiKeySource":"ANTHROPIC_API_KEY"` →
  `billing_error: "Credit balance is too low"` (HTTP 400, req_011CdqY5UM8MGJH995JkubgL).
  Root cause: the runner was launched with shell cwd inside `ai-dict`, so Bun auto-loaded
  the repo's `.env.local` — whose `ANTHROPIC_API_KEY` is a no-credit key — into the runner
  process, and every spawned executor session inherited it. This exact trap is already in
  session memory ("runner cwd/.env key trap", learned in a previous campaign) and was
  stepped on anyway because the launch command reused the shell cwd left over from the
  dry-run. **Workflow-improvement note:** the trap is documented in memory but not
  mechanically enforced — the runner itself could refuse/warn when `ANTHROPIC_API_KEY` is
  set in its environment, or unset it before spawning sessions.
- **[observation · runner-vs-doc gap] Undeclared dependencies run in PARALLEL, not
  sequentially.** The orchestrate-campaign skill text says "an undeclared dependency behaves
  as pure sequential order (today's default)" — empirically false: the runner spawned all 13
  dependency-free cards concurrently within 30 seconds. With the owner's explicit "one by
  one" directive this had to be corrected. Logged as a doc/behavior divergence for the tribe
  plugin fix-list.
- **[decision · Shaman] State reconciled + full sequential dependsOn chain declared** (each
  card depends on its sequence predecessor: B6→B9→A1→A3→B13→A2→A5→A13→A14→A7→A12→B14→B11→
  B10→B12→B15→A11). All dead cards reset to `staged` with `sessionId: null` (no work had
  happened — zero branches/worktrees existed). This encodes the owner's "one by one" and
  serializes merges, which the previous campaign already recommended.
- **[recovery · 15:45 UTC] Relaunch from safe cwd (`$CLAUDE_JOB_DIR/tmp`) with
  `env -u ANTHROPIC_API_KEY`** (task `bc1xe1nsg`). Dry-run re-verified `{B6, fresh}` first.
  New B6 session `cbc393d3` shows `apiKeySource: none` (Claude Code login) and is actively
  exploring the codebase. Exactly ONE card running — sequential chain honored.
- **[tick · 15:48 UTC] First watch-loop tick: healthy.** Lock held (runner PID 12264),
  B6 session log growing in real time, executor worktree `b6-words-page` created on branch
  `feature/B6WordsPage` from base `224b6d6`. No escalations yet. No action needed.
- **[card B6 · 16:51 UTC] PR #182 "[B6WordsPage] Words page" MERGED** at `4fec7bc4`,
  `gh pr checks` 14/14 green. Executor ran ~66 minutes end-to-end.
- **[escalation · 16:52 UTC] Runner exit 2: B6 `verify_failed_twice —
worktreeAndBranchGone`.** The executor merged and removed its worktree but left the remote
  branch `feature/B6WordsPage` on origin, so the runner's ship-verification failed twice and
  escalated instead of advancing to B9. All 16 downstream cards reported `blocked` (expected
  under the sequential chain).
- **[ruling · Shaman · R1] B6 adjudicated SHIPPED; unwritten convention UC-2 captured:**
  post-merge cleanup is part of Definition of Done — delete the remote feature branch, remove
  the worktree, fast-forward local master BEFORE reporting SHIPPED ("merged" ≠ "done"; done =
  next card starts clean on latest changes). Shaman performed the missing cleanup (remote
  branch deleted, local master fast-forwarded to `4fec7bc`), appended R1 to `answers.md`,
  and re-triggered the runner with `--include-escalated` (B6 auto-answer round 1 of 2).
  **Workflow-improvement note:** the executor brief apparently doesn't state the cleanup
  requirement explicitly, and the runner escalates rather than self-healing a
  remote-branch-only residue — both are tribe-plugin fix-list candidates.
- **[friction · workflow] Diary commit itself tripped the format gate:** the pre-commit hook
  rejected the R1 entry until `prettier --write` reformatted it — even a docs-only diary
  append pays the full repo-wide `format:check + lint` cost per commit (~40s). Minor, but it
  compounds across an append-only log; noted for the workflow review.
- **[tick · 16:55 UTC] Runner re-verified B6 → `shipped` (mergeSha `4fec7bc4` recorded) and
  advanced to B9 (backup/restore).** New run `16-53-53`, B9 session `ab8c2ec4` live (log
  mtime 1s old). Sequential chain holding: exactly one card running. Ruling R1's
  auto-answer round-trip worked end-to-end.
- **[incident · 18:17 UTC] Runner exit 3 — B9 session "ended incomplete" while
  deliberately waiting.** The B9 executor opened PR #183 ([B9BackupRestore] Backup &
  restore), armed a Monitor for CI checks + a background skinner re-verification, and ended
  its turn to wait for their notifications — the runner classified the turn-end as
  `session_incomplete` and exited. World state at diagnosis: PR #183 OPEN/MERGEABLE, CI
  green except `e2e-chrome` pending. **Workflow-improvement note (tribe fix-list): an
  executor that ends its turn to await async notifications (Monitor/background tasks) is
  indistinguishable from a dead session to the runner** — the SDK-side wait pattern and the
  runner's completion detection disagree. Recovery is cheap (exit 3 auto-resumes) but each
  occurrence costs a full runner round-trip.
- **[recovery · 18:19 UTC] Runner re-triggered** (task `bbiyb0e2e`) to resume B9 —
  no ruling needed, pure resume. The resume took the fresh-with-digest path (new session
  `adee46ec` rather than reviving `ab8c2ec4`), consistent with learning-spine-5's gap #2
  observation.
- **[card B9 · 18:41 UTC] PR #183 "[B9BackupRestore] Backup & restore" MERGED** at
  `8a61c5b8`. The runner verified ship **without escalation** — the R1/UC-2 cleanup
  convention (delete remote branch, remove worktree) held on its first post-ruling card,
  i.e. the answers.md digest is successfully steering executors. Runner advanced to A1.
- **[card A1 · 18:42 UTC] A1 (streamed answers) running** — the campaign's largest card
  (spec 60K + plan 80K). Owner note: the /loop directive was re-issued at 19:20 UTC; watch
  cron replaced (old `53b3deef` → new `f303d936`, same 15m cadence) to avoid double-firing.
- **[incident · 20:11 UTC] Runner exit 3 — SECOND turn-end-while-waiting occurrence, now on
  A1.** The A1 executor armed a fix-commit Monitor with a resumed Hunter running, ended its
  turn to await the event, and the runner again classified it `session_incomplete`. No PR
  open yet for A1. Re-triggered (task `bbfsx6n9e`), pure resume. **Pattern confirmed (2/2
  waiting cards): this is systematic, not incidental — the top tribe-plugin fix-list item
  from this session.** Cost so far: ~5 min dead time per occurrence.
- **[card A1 · 21:17 UTC] PR #184 "A1 — Streamed answers (progressive Gemini rendering)"
  MERGED** at `d803d6d9`, 14/14 checks green. Executor also did full R1/UC-2 cleanup
  unprompted (remote branch deleted, worktree removed) — the captured convention is sticking.
- **[escalation · 21:22 UTC] Runner exit 2: A1 `verify_failed_twice — schemaGuard`** —
  `ports.ts` changed since baseSha and the plan lacks `allowsSchemaChange: true`. Adjudicated
  as a **designed change, not drift**: the delta (`onChunk`/`renderPartial` ports +
  `lookup.chunk` wire message) is the literal subject of the A1 spec; the plan merely
  predates the runner's front-matter convention (2026-07-17 authoring wave vs. newer guard).
- **[ruling · Shaman · R2 + UC-3] A1 adjudicated SHIPPED; standing rule captured:** a
  schema-lock trip whose delta matches the card's own spec design (and passed both skinner
  lenses) gets `allowsSchemaChange` granted post-hoc; undesigned drift still escalates.
  Future executors should declare designed ports/wire changes in the PR body
  ("schema-lock opt-out: designed in spec §X"). Runner re-triggered with
  `--include-escalated` (task `b3oksjdoy`; A1 auto-answer round 1 of 2). Next: A3.
- **[correction · 21:35 UTC] The answers.md ruling alone could NOT clear the schemaGuard —
  the guard mechanically reads the PLAN FILE's front-matter, so the re-trigger exited 2 with
  the identical escalation.** Learning: for `verify_failed` escalations, an answers.md ruling
  fixes nothing unless the WORLD changes (escalation option 2). Executed the world-fix: PR
  #185 adds `allowsSchemaChange: true` front-matter to exactly the five plans that schedule
  explicit `Modify: packages/app/src/ports.ts` tasks (A1, A2, A3, A5, A12 — preempting four
  future identical escalations); A14/A7 stay guarded (their plans promise no ports.ts
  change). **Workflow-improvement note: the escalation file's "Options" list the ruling path
  first, but for mechanical verify failures only the world-fix path works — the runner could
  say which reasons are answerable vs. world-fixable.**
- **[world-fix landed · 21:57 UTC] PR #185 MERGED** at `2a73f7d`, 14/14 checks green
  (docs-only diff, ~20 min CI). Runner re-triggered with `--include-escalated`
  (task `b3fa4ceuz`) — A1's schemaGuard should now pass mechanically; expect A1 → shipped,
  then A3 (follow-up chips) to start.
- **[tick · 21:37 UTC] Confirmed: A1 verified `shipped` (mergeSha `d803d6d9`), A3 running**
  (session `1b4a4d3a`, log live). 3/17 shipped. A1 total cost: 2 sessions + 1
  turn-end-while-waiting resume + 1 schemaGuard escalation (world-fixed by PR #185).
- **[card A3 · 22:57 UTC] PR #186 "[A3FollowUpChips] Follow-up chips" MERGED at `8e830b31` —
  first ZERO-intervention card:** one session, no turn-end incident, no schemaGuard trip
  (PR #185's preemptive front-matter paid off), full cleanup, runner advanced straight to
  B13 (related words). 4/17 shipped. The convention-capture loop is measurably reducing
  friction: card interventions so far — B6: 1, B9: 1, A1: 2, A3: 0.
- **[card B13 · 23:59 UTC] PR #187 "[B13RelatedWords] Related words on save" MERGED at
  `74487930`**, checks green, cleanup done by the executor.
- **[escalation · 00:11 UTC 08-09] Runner exit 2: B13 schemaGuard — FALSE POSITIVE, and the
  root cause was the Shaman's own state reset.** The guard reported "ports.ts changed since
  `224b6d6`" but B13's PR touched ports.ts ZERO times: my post-first-launch reconciliation
  preserved the campaign-start baseSha on every card, and the runner never overwrites an
  existing baseSha — so B13's verify diffed from before A1's designed ports change. Same
  class as learning-spine-5 gap #3 (stale-base schemaGuard trips), reproduced from a new
  direction. **Reconciliation lesson captured (ruling R3): when resetting never-started
  cards, null `baseSha` too — a stale base is worse than no base.** Fixed: B13.baseSha set
  to its true base (`8e830b31`), all 12 future cards' stale baseSha nulled so the runner
  records real spawn-time bases. Runner re-triggered with `--include-escalated` (detached
  PID 12545 — no exit notification this cycle; the 15m cron is the sole watch signal).
- **[tick · 00:22 UTC] B13 verified shipped after the baseSha fix; A2 (recursive lookup)
  running.** 5/17.
- **[INCIDENT · severity: HIGH · 02:30 UTC] A2 executor MERGED PR #188 WITH A RED CHECK —
  the campaign's first violation of a WRITTEN rule.** `format-check` failed on the PR's
  final head `ec443d37` (misformatted `packages/extension-chrome/src/manifest.json`); the
  executor merged anyway at `dee39e11`, leaving MASTER RED on the format gate. Caught by the
  runner's post-merge `checksGreen` verify (exit 2, escalation A2). Unlike every prior event
  this is not an unwritten-convention gap — the CLAUDE.md merge gate is explicit. Reproduced
  locally: `bun run format:check` fails on master HEAD.
- **[ruling · Shaman · R4] (1) Master-red fix first:** PR #189 = pure `prettier --write` of
  manifest.json, opened 02:44 UTC, check-watcher armed; merge on full green. **(2) A2's
  PR-head check failure is historically immutable**, so after master greens, A2 will be
  hand-reconciled to `shipped` with R4 as the documented waiver (learning-spine-5 R8
  pattern: proof relocated to master HEAD, never waived). **(3) Standing reinforcement:**
  every check green BEFORE merge, pending ≠ green. **(4) Tribe fix-list: `checksGreen` runs
  post-merge only — the executor brief or a runner pre-merge hook should require the proof
  BEFORE `gh pr merge`.** Watch item: whether remaining cards honor the reinforced gate.
- **[resolution · 03:12 UTC] Master GREEN again: PR #189 merged at `bf80e84` (14/14),
  `prettier --check` clean on master HEAD.** A2 hand-reconciled to `shipped`
  (mergeSha `dee39e11`) per R4's relocated-proof waiver; escalation file archived as
  `A2.md.resolved-R4`. Dry-run confirms next action `{A5, fresh}`; runner relaunched
  (task `bl6t1otj1`). Master-red window: ~42 minutes (02:30 → 03:12 UTC). 6/17 shipped.
- **[incident · 02:51 UTC] External kill #2: the harness stopped background task
  `bl6t1otj1`, taking the real runner (PID 66756) and the just-spawned A5 session with it**
  (log frozen, PID gone; the earlier PR-#189 check-watcher was killed the same way).
  Relaunched immediately: new runner PID 68471 correctly detected the stale lock (dead
  holder) and took over; fresh A5 session `e0867eec` live within seconds. Recovery cost
  ~2 minutes. Same external-kill pattern the learning-spine-5 log recorded ("the rest were
  external kills of the background task") — chalked to harness task lifecycle, not the
  runner.
- **[card A5 · 04:31 UTC] PR #191 "[A5GlossMode] Gloss mode" MERGED at `682ecc0e` — clean
  ship, zero interventions**, merge gate honored (first card completed after the R4
  reinforcement). A13 (per-site quiet mode) running. 7/17 shipped. Intervention tally:
  B6 1 · B9 1 · A1 2 · A3 0 · B13 1 (self-inflicted) · A2 2 (incl. the HIGH merge-gate
  breach) · A5 0.
- **[card A13 · 06:03 UTC] PR #192 "[A13PerSiteQuietMode] Per-site quiet mode" MERGED at
  `471b8137` — clean ship, zero interventions.** A14 (double-click trigger) running.
  8/17 shipped; streak of 2 clean cards since the R4 reinforcement.
- **[card A14 · 07:02 UTC] PR #193 "[A14DoubleClickTrigger] Double-click trigger" MERGED at
  `8fbb441b` — clean ship, zero interventions.** A7 (pin cards) running. 9/17 shipped;
  clean streak now 3 (A5, A13, A14).
- **[card A7 · 09:18 UTC] PR #194 "[A7PinCards] Pin cards" MERGED at `9118a084` — clean
  ship, zero interventions** (longest clean card at ~2h11m). A12 (non-English source)
  running under E3 build-don't-advertise. 10/17 shipped; clean streak 4.
- **[correction nuance to R4] Project memory now records a "release-please manifest format
  trap": release-please leaves `manifest.json` prettier-dirty and feature PRs inherit the
  red format-check via the CI merge ref.** If that's the true origin of PR #188's red check,
  the A2 executor merged past a red gate that its own diff didn't cause — the GATE BREACH
  stands (a red check is never mergeable without a ruling), but the misformatting blame
  likely shifts from the A2 diff to release-please contamination. Kept as a nuance, not a
  reversal; the R4 standing reinforcement is unchanged.
- **[incident · ~09:55 UTC] Quota pause: the A12 session died on the account session limit**
  ("resets 5:10pm Asia/Saigon" = 10:10 UTC); runner exit 3. This is the exact scenario the
  15-minute cron heartbeat exists for (learning-spine-5 precedent: "survives usage-limit
  pauses, resumes on quota refresh"). Relaunched at 10:22 UTC, past the reset (task
  `bmynd5suq`); A12 resumes. Dead time ≈ 27 minutes.
- **[environment shift · 10:30 UTC] Master adopted a NEW written convention MID-CAMPAIGN
  (parallel owner activity, PR #195): Conventional Commits everywhere, NO `[CardName]`
  bracket prefixes** (they break release-please's parser — the same trap behind the R4
  nuance), enforced by a new `.githooks/commit-msg` + `check-conventional-commit.mjs` CI
  gate. Master also took #196 (gemini-sse fix) and release 1.11.2 — the campaign no longer
  has master to itself. **Every campaign card so far used bracket-style commits/PR titles;
  A12's in-flight branch carries 5+ bracket-prefixed commits that the new gate will
  reject.** Shaman response (R5): standing directive appended for all future briefs
  (Conventional form, examples given); A12 authorized to reword/rebase its commits
  (content unchanged) when the gate goes red — no interference with the live session.
  **Workflow observation: a convention can change UNDER a running campaign; the executor
  brief has no mechanism to receive mid-flight rule updates** (same digest-only gap as
  answers.md) — tribe fix-list.
- **[card A12 · 11:18 UTC] PR #198 "feat(a12): non-English source" MERGED at `0cba1c26` —
  shipped through BOTH mid-flight disruptions** (quota pause + the new conventional-commits
  CI gate; the resumed executor complied with R5, so the bracket-prefix rework happened
  without an escalation round). Built under E3 build-don't-advertise. B14 (sense-aware
  dedup) running. 11/17 shipped.
- **[escalation · 11:55 UTC] B14 NEEDS_DIRECTION — the campaign's FIRST genuine
  product-design escalation, and a model one.** The executor drove B14 to green
  (1127/1127 unit, 3/3 own e2e, builds pass) then STOPPED on a real cross-spec collision
  instead of merging with a red B13 e2e: B13 §2.6 ("normal re-save clears `related`",
  ratified) vs B14 §2.2/§4.1 ("exact sentence+url re-save = silent no-op, no write",
  ratified). B14's spec predates B13's ship, so neither spec saw the other. The executor
  explicitly cited R4 as the reason it refused to merge — the captured ruling changed
  executor behavior exactly as intended.
- **[ruling · Shaman · R6] B14 supersedes B13 §2.6 on the exact-duplicate path** — an exact
  dup is the SAME context so `related` is not stale; §2.6's staleness rationale only covers
  a different context, which B14 routes to a fresh sense with no `related`. Directions:
  update B13 unit+e2e expectations, annotate B13's spec §2.6 with a supersession note in
  the same PR, cite the ruling in the PR body, R4/R5 apply. Runner re-triggered with
  `--include-escalated` (task `b5vb90bxd`; B14 round 1 of 2).
- **[escalation · 12:40 UTC] B14 NEEDS_DIRECTION #2 — a SECOND distinct cross-spec gap,
  surfaced by Skinner B:** `saved.setRelated` hard-codes `senses[0]`, so once B14 enables
  multi-sense entries a related-tap on a non-first sense writes onto the wrong sense. B13's
  spec (lines 869–871) explicitly handed this obligation to "B14's own future spec" — but
  the batch-authored B14 spec never inherited it. R6 discharged cleanly (B13 e2e flipped,
  spec annotated); this is a new, uncovered scope call: ship dedup-only vs. expand B14 with
  an undesigned wire change. Executor recommendation: ship + defer.
- **[ruling · Shaman · R7 — FINAL auto-answer for B14 (round 2 of 2)] Ship B14 as-is;
  defer the sense-targeting fix as tracked debt.** Designing new wire addressing inside an
  implementer card is what the workflow forbids; the defect is latent, non-destructive,
  reversible. PR body must carry a "Known deferred defect (R7)" section. Shaman will
  propose follow-up card **"B16 — sense-targeted setRelated"** in the final owner report.
  **Root-cause capture for the tribe: batch-authored spec waves need an "inherited
  obligations" cross-check** — B13 flagged the handoff, B14's spec (authored same day)
  never received it. Runner re-triggered (task `b9kv63kq7`). If B14 escalates a third
  time it parks for the owner per wall W7.
- **[card B14 · 13:48 UTC] PR #200 "feat(b14): sense-aware dedup" MERGED at `b553782b` —
  both rulings discharged in the shipped PR** (R6: B13 e2e flipped + spec §2.6 supersession
  note; R7: "Known deferred defect" section in the PR body). The two-skinner audit earned
  its keep on this card: Skinner B found the cross-spec gap Skinner A missed. En route:
  third turn-end-while-waiting resume, plus a new operational lesson — **once a card has
  ever escalated, every re-trigger needs `--include-escalated` until it ships** (one
  flag-less relaunch parked B14 for a wasted cycle). B11 (casual review flip) running.
  12/17 shipped.
- **[checkpoint · 14:25 UTC 08-09] Owner requested context compaction; full resume state
  written to `.okra/runs/outstanding-17-2026-08-08/SHAMAN-STATE.md`** (scoreboard 12/17,
  rulings R0–R7, runner launch invariants, loop id `f303d936`, Stage-D checklist). A fresh
  or compacted session resumes from that file + this diary + the campaign home.
- **[card B11 · 15:08 UTC] PR #201 "feat(b11): casual review flip" MERGED at `2872453` —
  clean ship, zero escalations. Executor adaptation observed: it waited on CI in the
  FOREGROUND (`gh pr checks --watch`) explicitly to respect the liveness wall, avoiding the
  turn-end trap.** B10 (weekly digest) then started and hit that very trap anyway while
  waiting on its Hunter (4th occurrence); plain resume issued. 13/17 shipped — remaining:
  B10 (running) → B12 → B15 → A11-spike.
- **[card B10 · 16:33 UTC] PR #202 "feat(b10): weekly digest" MERGED at `43334947`** —
  clean after the one turn-end resume. B12 (LLM auto-grouping) running. 14/17 shipped —
  remaining: B12 → B15 → A11-spike.
- **[card B12 · 17:53 UTC] PR #203 "feat: LLM auto-grouping for saved words (B12)" MERGED
  at `abccade6` — clean ship, zero escalations,** foreground-CI-watch pattern again. B15
  (site lookup stats) running. 15/17 shipped — remaining: B15 → A11-spike.
- **[card B15 · 19:38 UTC] PR #204 "feat(b15): site lookup stats" MERGED at `56e67c60` —
  clean ship, zero escalations.** The FINAL card A11 (PDF spike) is running — E4 fence:
  spike deliverable only; the go/no-go and any `file://`/host permission are owner-only.
  16/17 shipped.
- **[card A11 · 20:23 UTC 08-09] PR #205 (PDF spike) MERGED at `2afd3fea` — runner exit 0:
  ran to done.** Spike deliverable only, per the E4 fence; the go/no-go and any
  `file://`/host permission remain owner decisions.
- **[Stage D · 20:30 UTC 08-09] Independent verification: ALL 17 CARDS PASS 3/3** via the
  `verify-shipped` skill (PR merged · local master == origin/master @ `2afd3fea` · card
  worktree removed) — PRs #182 #183 #184 #186 #187 #188 #191 #192 #193 #194 #198 #200 #201
  #202 #203 #204 #205. 50 commits carry the `Campaign: outstanding-17` trailer. Final stats:
  17 shipped, 0 escalated-parked, 0 blocked.

---

## Campaign wrap-up (for the workflow review this diary exists for)

**Outcome: 17/17 cards shipped and independently verified, zero owner interventions during
execution.** Elapsed ~29h wall-clock (2026-08-08 15:42 UTC → 2026-08-09 20:23 UTC) including
one quota pause and one GitHub-free overnight stretch.

**Rulings issued (answers.md):** R0 standing (2-skinner + UC capture) · R1/UC-2 post-merge
cleanup = DoD · R2/UC-3 designed schema changes get post-hoc opt-out · R3 null baseSha on
state resets · R4 merge gate reinforcement (after the one written-rule breach) · R5
Conventional Commits mid-flight adoption · R6 B14-supersedes-B13 §2.6 (real cross-spec
product call) · R7 ship-and-defer senses[0] setRelated (→ proposed follow-up card B16).

**Unwritten conventions captured this session:** UC-1 fresh worktrees need `bun install`
before any commit (hook is repo-wide) · UC-2 "merged" ≠ "done" — remote-branch/worktree
cleanup + master ff is part of shipping · UC-3 schema-guard opt-outs belong in plan
front-matter and "designed by the card's own spec" is the grant criterion · operational:
`--include-escalated` required on every re-trigger of an ever-escalated card.

**Tribe fix-list (ranked by observed cost):**

1. Executor-waits-look-dead (×4: B9, A1, B10, B14): a session that ends its turn awaiting a
   Monitor/background task is indistinguishable from a crash — runner needs a wait-aware
   liveness contract. Late-campaign executors adapted with foreground `gh pr checks --watch`.
2. `checksGreen` verifies AFTER merge — the gate needs mechanical enforcement BEFORE
   `gh pr merge` (the A2 breach + 42-min red master would have been prevented).
3. Escalation files don't distinguish answerable questions from world-fixable verify
   failures (one wasted round-trip on A1).
4. Briefs receive no mid-flight rule updates (conventional-commits gate landed under a
   running card; R5 digest reached only later cards).
5. Batch-authored spec waves need an inherited-obligations cross-check (B13→B14 handoff was
   dropped at authoring time; found by Skinner B).
6. Runner could refuse/unset `ANTHROPIC_API_KEY` from its environment (first-launch total
   session kill) and could self-heal remote-branch-only residue instead of escalating.

**Follow-up proposed to the owner:** new roadmap card **B16 "sense-targeted setRelated"**
(design: `b13-related-words-design.md:869-871` + B14's multi-sense model; deferred defect
documented in PR #200's body per R7).
